---
import { NAV_ITEMS, SITE_TITLE } from '../consts';
---

<header>
  <a class="site-title" href="/">
    {SITE_TITLE}
  </a>
  <nav>
    {NAV_ITEMS.map((item) => (
      <a href={item.href}>{item.label}</a>
    ))}
  </nav>
</header>

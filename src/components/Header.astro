---
import { NAV_ITEMS, SITE_TITLE } from '../consts';
---

<header transition:animate="none">
  <a class="site-title" href="/" transition:animate="none">
    {SITE_TITLE}
  </a>
  <nav>
    {NAV_ITEMS.map((item) => (
      <a href={item.href}>{item.label}</a>
    ))}
  </nav>
</header>
